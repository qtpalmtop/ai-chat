# WebView 加载地址（dev 默认指向 vue-version Koa SSR 服务的 3003 端口）
# vue-version 独立服务：`cd vue-version && npm run dev` 启动
# 切回 React 版：把端口改回 3001（NestJS 自身 SSR 渲染）
# 真机调试时改成 Mac 的 LAN IP，比如 http://192.168.1.100:3003
EXPO_PUBLIC_API_BASE_URL=http://localhost:3003
EXPO_PUBLIC_WEBVIEW_URL=http://localhost:3003/
